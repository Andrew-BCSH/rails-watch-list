<!DOCTYPE html>
<html>
<head>
  <title>List Details</title>
  <link rel="stylesheet" href="<%= asset_path('styles.css') %>">
</head>
<body>
  <div class="container">
    <h1>Shit Movies</h1>
    <% @lists.each do |list| %>
      <div class="list">
        <h3><%= list.name %></h3>
        <%= link_to "Show me the shit", list_path(list), class: "btn btn-primary" %>
    <%= render 'lists/frames/action' %>
    </div>
        <%= link_to "Show me the shit", list_path(list), class: "btn btn-primary" %>
    <%= render 'lists/frames/comedy' %>
    </div>
    <%= link_to "Show me the shit", list_path(list), class: "btn btn-primary" %>
    <%= render 'lists/frames/horror' %>
      </div>
    <% end %>
    <%= link_to "Add to the stink", new_list_path, class: "btn btn-secondary" %>
  </div>
</body>
</html>
